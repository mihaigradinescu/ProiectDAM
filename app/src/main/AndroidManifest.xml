<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ro.gradi.proiectv1">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".PubsApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.RegisterActivity" />
        <activity android:name=".ui.MyProfileActivity" />
        <activity android:name=".ui.ReviewActivity" />
        <activity android:name=".ui.ContactMeActivity" />
        <activity android:name=".ui.AboutActivity" />
        <activity android:name=".ui.Quiz1Activity" />
        <activity android:name=".ui.Quiz2Activity" />
        <activity android:name=".ui.Quiz3Activity" />
        <activity android:name=".ui.Quiz4Activity" />
        <activity
            android:name=".ui.HaveABeerInsteadActivity"
            android:label="Select a Pub" />
        <activity android:name=".ui.HistoryActivity"></activity>
    </application>

</manifest>